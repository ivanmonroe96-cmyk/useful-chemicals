---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getTranslations, t, localizedPath, getLangStaticPaths } from '../../i18n/index';
import { defaultLang } from '../../i18n/languages';

export const getStaticPaths = getLangStaticPaths;
const lang = Astro.params.lang ?? defaultLang;
const tr = getTranslations(lang);
const lp = (path: string) => localizedPath(path, lang);

const aboutSchema = {
  "@context": "https://schema.org",
  "@type": "AboutPage",
  "name": t(tr, "about.meta_title"),
  "description": t(tr, "about.meta_desc"),
  "mainEntity": {
    "@type": "Organization",
    "name": "UE Chemicals",
    "description": t(tr, 'site.organization_description'),
    "foundingLocation": { "@type": "Place", "name": "Bangkok, Thailand" },
    "address": { "@type": "PostalAddress", "addressLocality": "Bangkok", "postalCode": "10110", "addressCountry": "TH" },
    "email": "info@useful-chemicals.com",
    "knowsAbout": ["Caluanie Muelear Oxidize", "Red Liquid Mercury", "Silver Liquid Mercury", "Industrial Chemical Supply", "Chemical Quality Assurance"],
    "hasCredential": [
      { "@type": "EducationalOccupationalCredential", "credentialCategory": "ISO 9001:2015" },
      { "@type": "EducationalOccupationalCredential", "credentialCategory": "ISO 14001:2015" }
    ],
    "areaServed": { "@type": "Place", "name": "Worldwide" },
    "numberOfEmployees": { "@type": "QuantitativeValue", "minValue": 10 }
  },
  "review": [
    {
      "@type": "Review",
      "author": { "@type": "Person", "name": t(tr, "about.testimonial1_author") },
      "reviewBody": t(tr, "about.testimonial1_text"),
      "reviewRating": { "@type": "Rating", "ratingValue": "5" }
    },
    {
      "@type": "Review",
      "author": { "@type": "Person", "name": t(tr, "about.testimonial2_author") },
      "reviewBody": t(tr, "about.testimonial2_text"),
      "reviewRating": { "@type": "Rating", "ratingValue": "5" }
    },
    {
      "@type": "Review",
      "author": { "@type": "Person", "name": t(tr, "about.testimonial3_author") },
      "reviewBody": t(tr, "about.testimonial3_text"),
      "reviewRating": { "@type": "Rating", "ratingValue": "5" }
    },
    {
      "@type": "Review",
      "author": { "@type": "Person", "name": t(tr, "about.testimonial4_author") },
      "reviewBody": t(tr, "about.testimonial4_text"),
      "reviewRating": { "@type": "Rating", "ratingValue": "5" }
    }
  ]
};
---


<BaseLayout lang={lang} title={t(tr, "about.meta_title")} description={t(tr, "about.meta_desc")} activePage="about" jsonLd={aboutSchema}>

      <div class="ab-empathy-inner">
        <div class="ab-empathy-accent"></div>
        <div class="ab-empathy-content">
          <div class="section-label">{t(tr, "about.empathy_label")}</div>
          <h2>{t(tr, "about.empathy_title")}</h2>
          <p>{t(tr, "about.empathy_p1")}</p>
          <p>{t(tr, "about.empathy_p2")}</p>
          <p class="ab-empathy-highlight">{t(tr, "about.empathy_p3")}</p>
        </div>
      </div>
    </div>
  </section>

  <!-- ========== WHO WE ARE — IDENTITY & MISSION ========== -->
  <section class="ab-identity">
    <div class="ab-identity-image">
      <img src="/images/about/Whisk_a8e5707ae2a89769b4e4a5d84aafec92dr.png" alt="UE Chemicals manufacturing facility — industrial chemical production" loading="lazy" />
    </div>
    <div class="ab-identity-content">
      <div class="section-label">{t(tr, "about.identity_label")}</div>
      <h2>{t(tr, "about.identity_title")}</h2>
      <p>{t(tr, "about.identity_p1")}</p>
      <p>{t(tr, "about.identity_p2")}</p>
      <p>{t(tr, "about.identity_p3")}</p>
      <div class="ab-identity-features">
        <div class="ab-identity-feat">
          <div class="ab-identity-feat-icon"><i class="fas fa-industry"></i></div>
          <div>
            <h6>{t(tr, "about.identity_feat1_title")}</h6>
            <p>{t(tr, "about.identity_feat1_desc")}</p>
          </div>
        </div>
        <div class="ab-identity-feat">
          <div class="ab-identity-feat-icon"><i class="fas fa-heart"></i></div>
          <div>
            <h6>{t(tr, "about.identity_feat2_title")}</h6>
            <p>{t(tr, "about.identity_feat2_desc")}</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ========== PROOF OF DELIVERY ========== -->
  <section class="ab-delivery">
    <div class="container">
      <div class="ab-delivery-grid">
        <div class="ab-delivery-content">
          <div class="section-label">{t(tr, "about.delivery_label")}</div>
          <h2>{t(tr, "about.delivery_title")}</h2>
          <p>{t(tr, "about.delivery_p1")}</p>
          <p>{t(tr, "about.delivery_p2")}</p>
        </div>
        <div class="ab-delivery-cards">
          <div class="ab-delivery-card">
            <div class="ab-delivery-card-icon"><i class="fas fa-location-dot"></i></div>
            <div>
              <h5>{t(tr, "about.delivery_item1_title")}</h5>
              <p>{t(tr, "about.delivery_item1_desc")}</p>
            </div>
          </div>
          <div class="ab-delivery-card">
            <div class="ab-delivery-card-icon"><i class="fas fa-box"></i></div>
            <div>
              <h5>{t(tr, "about.delivery_item2_title")}</h5>
              <p>{t(tr, "about.delivery_item2_desc")}</p>
            </div>
          </div>
          <div class="ab-delivery-card">
            <div class="ab-delivery-card-icon"><i class="fas fa-truck-fast"></i></div>
            <div>
              <h5>{t(tr, "about.delivery_item3_title")}</h5>
              <p>{t(tr, "about.delivery_item3_desc")}</p>
            </div>
          </div>
          <div class="ab-delivery-card">
            <div class="ab-delivery-card-icon"><i class="fas fa-file-lines"></i></div>
            <div>
              <h5>{t(tr, "about.delivery_item4_title")}</h5>
              <p>{t(tr, "about.delivery_item4_desc")}</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ========== QUALITY ASSURANCE PROCESS ========== -->
  <section class="ab-quality">
    <div class="container">
      <div class="ab-quality-header">
        <div class="section-label">{t(tr, "about.quality_label")}</div>
        <h2>{t(tr, "about.quality_title")}</h2>
        <p>{t(tr, "about.quality_p1")}</p>
      </div>
      <div class="ab-quality-steps">
        <div class="ab-quality-step">
          <div class="ab-quality-step-num">01</div>
          <div class="ab-quality-step-body">
            <h5>{t(tr, "about.quality_step1_title")}</h5>
            <p>{t(tr, "about.quality_step1_desc")}</p>
          </div>
        </div>
        <div class="ab-quality-step">
          <div class="ab-quality-step-num">02</div>
          <div class="ab-quality-step-body">
            <h5>{t(tr, "about.quality_step2_title")}</h5>
            <p>{t(tr, "about.quality_step2_desc")}</p>
          </div>
        </div>
        <div class="ab-quality-step">
          <div class="ab-quality-step-num">03</div>
          <div class="ab-quality-step-body">
            <h5>{t(tr, "about.quality_step3_title")}</h5>
            <p>{t(tr, "about.quality_step3_desc")}</p>
          </div>
        </div>
        <div class="ab-quality-step">
          <div class="ab-quality-step-num">04</div>
          <div class="ab-quality-step-body">
            <h5>{t(tr, "about.quality_step4_title")}</h5>
            <p>{t(tr, "about.quality_step4_desc")}</p>
          </div>
        </div>
        <div class="ab-quality-step">
          <div class="ab-quality-step-num">05</div>
          <div class="ab-quality-step-body">
            <h5>{t(tr, "about.quality_step5_title")}</h5>
            <p>{t(tr, "about.quality_step5_desc")}</p>
          </div>
        </div>
      </div>
      <div class="ab-quality-coa">
        <i class="fas fa-certificate"></i>
        <p>{t(tr, "about.quality_coa")}</p>
      </div>
    </div>
  </section>

  <!-- ========== CUSTOMER FOCUS ========== -->
  <section class="ab-customer">
    <div class="ab-customer-image">
      <img src="/images/about/BTC-CHONBURI-PLANT-2-5.6-scaled.jpg" alt="UE Chemicals customer service and support team" loading="lazy" />
    </div>
    <div class="ab-customer-content">
      <div class="section-label">{t(tr, "about.customer_label")}</div>
      <h2>{t(tr, "about.customer_title")}</h2>
      <p>{t(tr, "about.customer_p1")}</p>
      <div class="ab-customer-items">
        <div class="ab-customer-item">
          <div class="ab-customer-item-icon"><i class="fas fa-clock"></i></div>
          <div>
            <h6>{t(tr, "about.customer_item1_title")}</h6>
            <p>{t(tr, "about.customer_item1_desc")}</p>
          </div>
        </div>
        <div class="ab-customer-item">
          <div class="ab-customer-item-icon"><i class="fas fa-user-tie"></i></div>
          <div>
            <h6>{t(tr, "about.customer_item2_title")}</h6>
            <p>{t(tr, "about.customer_item2_desc")}</p>
          </div>
        </div>
        <div class="ab-customer-item">
          <div class="ab-customer-item-icon"><i class="fas fa-sliders"></i></div>
          <div>
            <h6>{t(tr, "about.customer_item3_title")}</h6>
            <p>{t(tr, "about.customer_item3_desc")}</p>
          </div>
        </div>
        <div class="ab-customer-item">
          <div class="ab-customer-item-icon"><i class="fas fa-comments"></i></div>
          <div>
            <h6>{t(tr, "about.customer_item4_title")}</h6>
            <p>{t(tr, "about.customer_item4_desc")}</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ========== TESTIMONIALS ========== -->
  <section class="ab-testimonials">
    <div class="container">
      <div class="ab-testimonials-header">
        <div class="section-label">{t(tr, "about.testimonials_label")}</div>
        <h2>{t(tr, "about.testimonials_title")}</h2>
      </div>
      <div class="ab-testimonials-grid">
        <div class="ab-testimonial-card">
          <div class="ab-testimonial-stars">
            <i class="fas fa-star"></i><i class="fas fa-star"></i><i class="fas fa-star"></i><i class="fas fa-star"></i><i class="fas fa-star"></i>
          </div>
          <blockquote>{t(tr, "about.testimonial1_text")}</blockquote>
          <div class="ab-testimonial-author">
            <div class="ab-testimonial-avatar"><i class="fas fa-user"></i></div>
            <div>
              <strong>{t(tr, "about.testimonial1_author")}</strong>
              <span>{t(tr, "about.testimonial1_location")}</span>
            </div>
          </div>
        </div>
        <div class="ab-testimonial-card">
          <div class="ab-testimonial-stars">
            <i class="fas fa-star"></i><i class="fas fa-star"></i><i class="fas fa-star"></i><i class="fas fa-star"></i><i class="fas fa-star"></i>
          </div>
          <blockquote>{t(tr, "about.testimonial2_text")}</blockquote>
          <div class="ab-testimonial-author">
            <div class="ab-testimonial-avatar"><i class="fas fa-user"></i></div>
            <div>
              <strong>{t(tr, "about.testimonial2_author")}</strong>
              <span>{t(tr, "about.testimonial2_location")}</span>
            </div>
          </div>
        </div>
        <div class="ab-testimonial-card">
          <div class="ab-testimonial-stars">
            <i class="fas fa-star"></i><i class="fas fa-star"></i><i class="fas fa-star"></i><i class="fas fa-star"></i><i class="fas fa-star"></i>
          </div>
          <blockquote>{t(tr, "about.testimonial3_text")}</blockquote>
          <div class="ab-testimonial-author">
            <div class="ab-testimonial-avatar"><i class="fas fa-user"></i></div>
            <div>
              <strong>{t(tr, "about.testimonial3_author")}</strong>
              <span>{t(tr, "about.testimonial3_location")}</span>
            </div>
          </div>
        </div>
        <div class="ab-testimonial-card">
          <div class="ab-testimonial-stars">
            <i class="fas fa-star"></i><i class="fas fa-star"></i><i class="fas fa-star"></i><i class="fas fa-star"></i><i class="fas fa-star"></i>
          </div>
          <blockquote>{t(tr, "about.testimonial4_text")}</blockquote>
          <div class="ab-testimonial-author">
            <div class="ab-testimonial-avatar"><i class="fas fa-user"></i></div>
            <div>
              <strong>{t(tr, "about.testimonial4_author")}</strong>
              <span>{t(tr, "about.testimonial4_location")}</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ========== TRANSPARENCY ========== -->
  <section class="ab-transparency">
    <div class="container">
      <div class="ab-transparency-inner">
        <div class="ab-transparency-content">
          <div class="section-label">{t(tr, "about.transparency_label")}</div>
          <h2>{t(tr, "about.transparency_title")}</h2>
          <p>{t(tr, "about.transparency_p1")}</p>
        </div>
        <ul class="ab-transparency-list">
          <li><i class="fas fa-check"></i> <span>{t(tr, "about.transparency_item1")}</span></li>
          <li><i class="fas fa-check"></i> <span>{t(tr, "about.transparency_item2")}</span></li>
          <li><i class="fas fa-check"></i> <span>{t(tr, "about.transparency_item3")}</span></li>
          <li><i class="fas fa-check"></i> <span>{t(tr, "about.transparency_item4")}</span></li>
          <li><i class="fas fa-check"></i> <span>{t(tr, "about.transparency_item5")}</span></li>
        </ul>
      </div>
    </div>
  </section>

  <!-- ========== CORE PRODUCTS ========== -->
  <section class="ab-products">
    <div class="container">
      <div class="ab-section-header">
        <div>
          <div class="section-label">{t(tr, "about.products_label")}</div>
          <h2>{t(tr, "about.products_title")}</h2>
        </div>
        <a href={lp("/products")} class="btn btn-outline">{t(tr, "common.view_all_products")} <i class="fas fa-arrow-right"></i></a>
      </div>
      <div class="ab-products-grid">
        <a href={lp("/products/caluanie")} class="ab-prod-card">
          <div class="ab-prod-img">
            <img loading="lazy" src="/images/home-caluanie-product.jpeg" alt="Buy Caluanie Muelear Oxidize — premium quality for sale" />
            <div class="ab-prod-num">01</div>
          </div>
          <div class="ab-prod-body">
            <h3>{t(tr, "about.prod1_name")}</h3>
            <p>{t(tr, "about.prod1_desc")}</p>
            <span class="ab-prod-link">{t(tr, "common.learn_more")} <i class="fas fa-arrow-right"></i></span>
          </div>
        </a>
        <a href={lp("/products/red-mercury")} class="ab-prod-card">
          <div class="ab-prod-img">
            <img loading="lazy" src="/images/home-red-mercury-product.jpeg" alt="Red Liquid Mercury for sale — laboratory grade" />
            <div class="ab-prod-num">02</div>
          </div>
          <div class="ab-prod-body">
            <h3>{t(tr, "about.prod2_name")}</h3>
            <p>{t(tr, "about.prod2_desc")}</p>
            <span class="ab-prod-link">{t(tr, "common.learn_more")} <i class="fas fa-arrow-right"></i></span>
          </div>
        </a>
        <a href={lp("/products/silver-mercury")} class="ab-prod-card">
          <div class="ab-prod-img">
            <img loading="lazy" src="/images/home-silver-mercury-product.jpeg" alt="Silver Liquid Mercury 99.99% purity for sale" />
            <div class="ab-prod-num">03</div>
          </div>
          <div class="ab-prod-body">
            <h3>{t(tr, "about.prod3_name")}</h3>
            <p>{t(tr, "about.prod3_desc")}</p>
            <span class="ab-prod-link">{t(tr, "common.learn_more")} <i class="fas fa-arrow-right"></i></span>
          </div>
        </a>
      </div>
    </div>
  </section>

  <!-- ========== FACILITY ========== -->
  <section class="ab-facility">
    <div class="ab-facility-content">
      <div class="section-label">{t(tr, "about.facility_label")}</div>
      <h2>{t(tr, "about.facility_title")}</h2>
      <p>{t(tr, "about.facility_p1")}</p>
      <p>{t(tr, "about.facility_p2")}</p>
      <a href={lp("/certificates")} class="btn btn-primary">{t(tr, "footer.certificates")} <i class="fas fa-arrow-right"></i></a>
    </div>
    <div class="ab-facility-image">
      <img loading="lazy" src="/images/about/BTC-P2-1.jpg" alt="UE Chemicals manufacturing facility in Bangkok, Thailand" />
    </div>
  </section>

  <!-- ========== TEAM ========== -->
  <section class="ab-team">
    <div class="container">
      <div class="ab-team-inner">
        <div class="ab-team-text">
          <div class="section-label">{t(tr, "about.team_label")}</div>
          <h2>{t(tr, "about.team_title")}</h2>
          <p>{t(tr, "about.team_desc")}</p>
        </div>
        <div class="ab-team-card">
          <div class="ab-team-avatar"><i class="fas fa-user"></i></div>
          <div class="ab-team-info">
            <h4>{t(tr, "about.team_name")}</h4>
            <span class="ab-team-role">{t(tr, "about.team_role")}</span>
            <p>{t(tr, "about.team_bio")}</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ========== CTA ========== -->
  <section class="ab-cta">
    <div class="container">
      <div class="ab-cta-inner">
        <div class="ab-cta-content">
          <h2>{t(tr, "about.cta_title")}</h2>
          <p>{t(tr, "about.cta_desc")}</p>
        </div>
        <div class="ab-cta-actions">
          <a href={lp("/contact")} class="btn btn-white">{t(tr, "about.cta_btn_contact")} <i class="fas fa-arrow-right"></i></a>
          <a href={lp("/ordering")} class="btn btn-outline-white">{t(tr, "about.cta_btn_order")} <i class="fas fa-arrow-right"></i></a>
          <a href={lp("/products")} class="btn btn-outline-white">{t(tr, "about.cta_btn_products")} <i class="fas fa-arrow-right"></i></a>
        </div>
      </div>
    </div>
  </section>

</BaseLayout>

<style>
  /* Hero */
  .ab-hero { position: relative; min-height: 85vh; display: flex; align-items: center; overflow: hidden; }
  .ab-hero-bg { position: absolute; inset: 0; z-index: 0; }
  .ab-hero-bg img { width: 100%; height: 100%; object-fit: cover; }
  .ab-hero-overlay { position: absolute; inset: 0; background: linear-gradient(135deg, rgba(25,24,23,0.93) 0%, rgba(25,24,23,0.75) 50%, rgba(252,82,32,0.12) 100%); }
  .ab-hero-inner { position: relative; z-index: 1; padding-top: 140px; padding-bottom: 100px; }
  .ab-hero-content { max-width: 720px; }
  .ab-hero-badge { display: inline-flex; align-items: center; gap: 10px; padding: 8px 20px; border: 1px solid rgba(255,255,255,0.2); font-size: 12px; font-weight: 700; text-transform: uppercase; letter-spacing: 2px; color: rgba(255,255,255,0.8); margin-bottom: 24px; }
  .ab-hero-badge i { color: var(--accent); font-size: 13px; }
  .ab-hero h1 { color: var(--white); font-size: clamp(32px, 4.5vw, 58px); line-height: 1.1; margin-bottom: 24px; letter-spacing: -0.5px; }
  .ab-hero-sub { color: rgba(255,255,255,0.7); font-size: 18px; line-height: 1.7; max-width: 600px; margin-bottom: 40px; }
  .ab-hero-actions { display: flex; gap: 16px; flex-wrap: wrap; }
  .btn-outline-white { background: transparent; color: var(--white); border: 2px solid rgba(255,255,255,0.3); }
  .btn-outline-white:hover { background: var(--white); color: var(--near-black); border-color: var(--white); }
  .ab-hero-stats { display: flex; align-items: center; gap: 40px; margin-top: 80px; padding-top: 40px; border-top: 1px solid rgba(255,255,255,0.12); }
  .ab-hero-stat-num { display: block; font-size: 28px; font-weight: 800; color: var(--white); letter-spacing: -0.5px; }
  .ab-hero-stat-label { display: block; font-size: 12px; color: rgba(255,255,255,0.5); text-transform: uppercase; letter-spacing: 1px; margin-top: 4px; }
  .ab-hero-stat-divider { width: 1px; height: 50px; background: rgba(255,255,255,0.12); }

  /* Empathy Section */
  .ab-empathy { padding: 100px 0; background: var(--white); }
  .ab-empathy-inner { display: flex; gap: 40px; max-width: 840px; margin: 0 auto; }
  .ab-empathy-accent { width: 4px; background: var(--accent); flex-shrink: 0; border-radius: 2px; }
  .ab-empathy-content h2 { font-size: clamp(24px, 3vw, 36px); line-height: 1.2; margin-bottom: 24px; }
  .ab-empathy-content p { margin-bottom: 16px; line-height: 1.8; }
  .ab-empathy-highlight { font-weight: 500; color: var(--near-black); font-style: italic; border-left: 3px solid var(--accent); padding-left: 20px; margin-top: 24px; }

  /* Identity / Who We Are Split */
  .ab-identity { display: grid; grid-template-columns: 1fr 1fr; min-height: 650px; }
  .ab-identity-image { overflow: hidden; }
  .ab-identity-image img { width: 100%; height: 100%; object-fit: cover; }
  .ab-identity-content { padding: 80px 60px; display: flex; flex-direction: column; justify-content: center; background: var(--light-bg); }
  .ab-identity-content h2 { font-size: clamp(22px, 2.5vw, 34px); margin-bottom: 20px; line-height: 1.2; }
  .ab-identity-content > p { max-width: 500px; margin-bottom: 14px; line-height: 1.7; }
  .ab-identity-features { margin: 24px 0 0; display: flex; flex-direction: column; gap: 24px; }
  .ab-identity-feat { display: flex; gap: 16px; align-items: flex-start; }
  .ab-identity-feat-icon { width: 48px; height: 48px; background: var(--near-black); display: flex; align-items: center; justify-content: center; color: var(--white); flex-shrink: 0; font-size: 18px; }
  .ab-identity-feat h6 { font-size: 15px; margin-bottom: 4px; }
  .ab-identity-feat p { font-size: 14px; margin: 0; color: var(--body-text); }

  /* Delivery / Proof Section */
  .ab-delivery { padding: 120px 0; background: var(--light-bg); border-top: 1px solid var(--border); }
  .ab-delivery-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 80px; align-items: start; }
  .ab-delivery-content h2 { font-size: clamp(24px, 3vw, 40px); margin-bottom: 20px; line-height: 1.15; }
  .ab-delivery-content p { margin-bottom: 16px; line-height: 1.7; }
  .ab-delivery-cards { display: flex; flex-direction: column; }
  .ab-delivery-card { display: flex; gap: 20px; padding: 24px 0; border-bottom: 1px solid var(--border); align-items: flex-start; }
  .ab-delivery-card:first-child { padding-top: 0; }
  .ab-delivery-card:last-child { border-bottom: none; }
  .ab-delivery-card-icon { width: 44px; height: 44px; background: var(--near-black); display: flex; align-items: center; justify-content: center; color: var(--white); flex-shrink: 0; font-size: 16px; }
  .ab-delivery-card h5 { font-size: 15px; margin-bottom: 4px; }
  .ab-delivery-card p { font-size: 14px; margin: 0; color: var(--body-text); line-height: 1.6; }

  /* Quality Assurance Process */
  .ab-quality { padding: 120px 0; background: var(--white); }
  .ab-quality-header { text-align: center; max-width: 720px; margin: 0 auto 60px; }
  .ab-quality-header h2 { font-size: clamp(26px, 3.5vw, 44px); line-height: 1.15; margin-bottom: 20px; }
  .ab-quality-header p { line-height: 1.7; }
  .ab-quality-steps { display: grid; grid-template-columns: repeat(5, 1fr); gap: 0; margin-bottom: 50px; }
  .ab-quality-step { padding: 30px 24px; border-left: 1px solid var(--border); position: relative; }
  .ab-quality-step:last-child { border-right: 1px solid var(--border); }
  .ab-quality-step::before { content: ''; position: absolute; top: 0; left: 0; right: 0; height: 3px; background: var(--accent); transform: scaleX(0); transition: transform 0.4s ease; }
  .ab-quality-step:hover::before { transform: scaleX(1); }
  .ab-quality-step-num { font-size: 14px; font-weight: 800; color: var(--accent); letter-spacing: 1px; margin-bottom: 16px; }
  .ab-quality-step-body h5 { font-size: 15px; margin-bottom: 8px; text-transform: uppercase; }
  .ab-quality-step-body p { font-size: 13px; color: var(--body-text); margin: 0; line-height: 1.6; }
  .ab-quality-coa { display: flex; align-items: center; gap: 20px; background: var(--light-bg); padding: 30px 40px; border-left: 4px solid var(--accent); }
  .ab-quality-coa i { font-size: 28px; color: var(--accent); flex-shrink: 0; }
  .ab-quality-coa p { font-size: 16px; font-weight: 500; margin: 0; line-height: 1.6; color: var(--near-black); }

  /* Customer Focus Split */
  .ab-customer { display: grid; grid-template-columns: 1fr 1fr; min-height: 650px; }
  .ab-customer-image { overflow: hidden; }
  .ab-customer-image img { width: 100%; height: 100%; object-fit: cover; }
  .ab-customer-content { padding: 80px 60px; display: flex; flex-direction: column; justify-content: center; background: var(--near-black); }
  .ab-customer-content .section-label { color: rgba(255,255,255,0.6); }
  .ab-customer-content .section-label::before { background: var(--accent); }
  .ab-customer-content h2 { color: var(--white); font-size: clamp(22px, 2.5vw, 34px); margin-bottom: 16px; line-height: 1.2; }
  .ab-customer-content > p { color: rgba(255,255,255,0.65); max-width: 500px; margin-bottom: 28px; line-height: 1.7; }
  .ab-customer-items { display: flex; flex-direction: column; gap: 20px; }
  .ab-customer-item { display: flex; gap: 16px; align-items: flex-start; }
  .ab-customer-item-icon { width: 40px; height: 40px; background: rgba(255,255,255,0.08); display: flex; align-items: center; justify-content: center; color: var(--accent); flex-shrink: 0; font-size: 16px; }
  .ab-customer-item h6 { color: var(--white); font-size: 14px; margin-bottom: 4px; }
  .ab-customer-item p { font-size: 13px; margin: 0; color: rgba(255,255,255,0.5); line-height: 1.5; }

  /* Testimonials */
  .ab-testimonials { padding: 120px 0; background: var(--light-bg); border-top: 1px solid var(--border); }
  .ab-testimonials-header { text-align: center; margin-bottom: 60px; }
  .ab-testimonials-header h2 { font-size: clamp(26px, 3.5vw, 44px); line-height: 1.15; }
  .ab-testimonials-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 30px; }
  .ab-testimonial-card { background: var(--white); padding: 40px; border: 1px solid var(--border); transition: var(--transition); }
  .ab-testimonial-card:hover { box-shadow: 0 8px 30px rgba(0,0,0,0.06); transform: translateY(-2px); }
  .ab-testimonial-stars { margin-bottom: 20px; }
  .ab-testimonial-stars i { color: #f5a623; font-size: 14px; margin-right: 2px; }
  .ab-testimonial-card blockquote { font-size: 15px; line-height: 1.75; color: var(--body-text); margin: 0 0 24px; font-style: italic; }
  .ab-testimonial-author { display: flex; align-items: center; gap: 14px; }
  .ab-testimonial-avatar { width: 44px; height: 44px; background: var(--near-black); border-radius: 50%; display: flex; align-items: center; justify-content: center; color: var(--white); font-size: 16px; flex-shrink: 0; }
  .ab-testimonial-author strong { display: block; font-size: 14px; color: var(--near-black); }
  .ab-testimonial-author span { display: block; font-size: 12px; color: var(--body-text); margin-top: 2px; }

  /* Transparency */
  .ab-transparency { padding: 100px 0; background: var(--white); }
  .ab-transparency-inner { display: grid; grid-template-columns: 1fr 1fr; gap: 60px; align-items: start; }
  .ab-transparency-content h2 { font-size: clamp(24px, 3vw, 40px); margin-bottom: 16px; line-height: 1.15; }
  .ab-transparency-content p { line-height: 1.7; }
  .ab-transparency-list { list-style: none; padding: 0; margin: 0; }
  .ab-transparency-list li { display: flex; gap: 14px; align-items: flex-start; padding: 16px 0; border-bottom: 1px solid var(--border); font-size: 15px; line-height: 1.6; }
  .ab-transparency-list li:first-child { padding-top: 0; }
  .ab-transparency-list li:last-child { border-bottom: none; }
  .ab-transparency-list i { color: var(--accent); font-size: 14px; margin-top: 4px; flex-shrink: 0; }

  /* Products Grid */
  .ab-products { padding: 120px 0; background: var(--light-bg); border-top: 1px solid var(--border); }
  .ab-section-header { display: flex; justify-content: space-between; align-items: flex-end; gap: 30px; margin-bottom: 60px; }
  .ab-section-header h2 { font-size: clamp(28px, 3.5vw, 50px); line-height: 1.15; }
  .ab-products-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 0; }
  .ab-prod-card { display: block; border-left: 1px solid var(--border); overflow: hidden; transition: var(--transition); color: var(--body-text); background: var(--white); }
  .ab-prod-card:last-child { border-right: 1px solid var(--border); }
  .ab-prod-img { position: relative; height: 300px; overflow: hidden; }
  .ab-prod-img img { width: 100%; height: 100%; object-fit: cover; transition: transform 0.6s ease; }
  .ab-prod-card:hover .ab-prod-img img { transform: scale(1.08); }
  .ab-prod-num { position: absolute; top: 20px; left: 20px; font-size: 13px; font-weight: 800; color: var(--white); background: var(--near-black); padding: 6px 14px; letter-spacing: 1px; }
  .ab-prod-body { padding: 30px; }
  .ab-prod-body h3 { font-size: 20px; margin-bottom: 10px; transition: color 0.3s; }
  .ab-prod-card:hover .ab-prod-body h3 { color: var(--accent); }
  .ab-prod-body p { font-size: 14px; margin-bottom: 20px; line-height: 1.6; }
  .ab-prod-link { font-size: 13px; font-weight: 700; text-transform: uppercase; letter-spacing: 1.5px; color: var(--accent); display: inline-flex; align-items: center; gap: 8px; }
  .ab-prod-link i { font-size: 11px; transition: transform 0.3s; }
  .ab-prod-card:hover .ab-prod-link i { transform: translateX(6px); }

  /* Facility Split */
  .ab-facility { display: grid; grid-template-columns: 1fr 1fr; min-height: 560px; }
  .ab-facility-content { padding: 80px 60px; display: flex; flex-direction: column; justify-content: center; background: var(--white); }
  .ab-facility-content h2 { font-size: clamp(24px, 3vw, 38px); margin-bottom: 20px; line-height: 1.15; }
  .ab-facility-content > p { max-width: 480px; margin-bottom: 14px; line-height: 1.7; }
  .ab-facility-content .btn { margin-top: 20px; align-self: flex-start; }
  .ab-facility-image { overflow: hidden; }
  .ab-facility-image img { width: 100%; height: 100%; object-fit: cover; }

  /* Team */
  .ab-team { padding: 100px 0; background: var(--light-bg); border-top: 1px solid var(--border); }
  .ab-team-inner { display: grid; grid-template-columns: 1fr 1fr; gap: 60px; align-items: center; }
  .ab-team-text h2 { font-size: clamp(24px, 3vw, 42px); margin-bottom: 16px; line-height: 1.15; }
  .ab-team-text p { max-width: 440px; line-height: 1.7; }
  .ab-team-card { display: flex; gap: 24px; align-items: center; padding: 40px; border: 1px solid var(--border); background: var(--white); }
  .ab-team-avatar { width: 80px; height: 80px; background: var(--near-black); display: flex; align-items: center; justify-content: center; font-size: 2rem; color: var(--white); flex-shrink: 0; border-radius: 50%; }
  .ab-team-info h4 { font-size: 20px; margin-bottom: 4px; }
  .ab-team-role { font-size: 13px; font-weight: 700; text-transform: uppercase; letter-spacing: 1px; color: var(--accent); display: block; margin-bottom: 10px; }
  .ab-team-info p { font-size: 14px; color: var(--body-text); margin: 0; line-height: 1.6; }

  /* CTA */
  .ab-cta { padding: 100px 0; background: var(--accent); }
  .ab-cta-inner { display: flex; justify-content: space-between; align-items: center; gap: 40px; }
  .ab-cta-content h2 { color: var(--white); font-size: clamp(24px, 3vw, 40px); margin-bottom: 12px; line-height: 1.2; }
  .ab-cta-content p { color: rgba(255,255,255,0.85); max-width: 520px; margin: 0; line-height: 1.7; }
  .ab-cta-actions { display: flex; gap: 14px; flex-shrink: 0; flex-wrap: wrap; }
  .ab-cta .btn-white { border-color: var(--white); }
  .ab-cta .btn-white:hover { background: var(--near-black); color: var(--white); border-color: var(--near-black); }
  .ab-cta .btn-outline-white { border-color: rgba(255,255,255,0.4); }
  .ab-cta .btn-outline-white:hover { background: var(--white); color: var(--accent); border-color: var(--white); }

  /* Refined visual system */
  .ab-hero { background: var(--primary-dark); }
  .ab-hero-overlay { background: linear-gradient(115deg, rgba(8,47,56,0.96) 0%, rgba(15,76,92,0.78) 54%, rgba(47,143,107,0.24) 100%); }
  .ab-hero-badge { border-radius: 8px; background: rgba(255,255,255,0.1); border-color: rgba(255,255,255,0.18); backdrop-filter: blur(10px); }
  .ab-hero-badge i { color: var(--accent); }
  .ab-hero-sub { color: rgba(255,255,255,0.78); }
  .ab-hero-stats { width: fit-content; max-width: 100%; padding: 24px 28px; background: rgba(255,255,255,0.08); border: 1px solid rgba(255,255,255,0.14); border-radius: 8px; backdrop-filter: blur(10px); }
  .ab-hero-stat-label { color: rgba(255,255,255,0.66); }
  .ab-empathy { background: var(--surface); }
  .ab-empathy-inner { background: var(--white); border: 1px solid var(--border); border-radius: 8px; padding: 42px; box-shadow: var(--shadow-sm); }
  .ab-empathy-accent { background: var(--secondary); }
  .ab-identity-content,
  .ab-delivery,
  .ab-products,
  .ab-team { background: linear-gradient(135deg, var(--surface), var(--secondary-soft)); }
  .ab-identity-feat-icon,
  .ab-delivery-card-icon,
  .ab-customer-item-icon,
  .ab-team-avatar { border-radius: 8px; background: var(--primary-dark); color: var(--accent); }
  .ab-quality-step { background: var(--white); }
  .ab-quality-step::before { background: var(--secondary); }
  .ab-quality-coa,
  .ab-team-card,
  .ab-testimonial-card { border-radius: 8px; box-shadow: var(--shadow-sm); }
  .ab-customer-content { background: linear-gradient(135deg, var(--primary-dark), #0B3E49); }
  .ab-testimonials { background: var(--surface); }
  .ab-transparency-list li { background: var(--white); padding: 16px 18px; border: 1px solid var(--border); border-radius: 8px; margin-bottom: 10px; }
  .ab-prod-card { border: 1px solid var(--border); border-radius: 8px; box-shadow: var(--shadow-sm); }
  .ab-prod-card:last-child { border-right: 1px solid var(--border); }
  .ab-prod-card:hover { transform: translateY(-6px); box-shadow: var(--shadow-md); border-color: rgba(252,82,32,0.35); }
  .ab-prod-num { border-radius: 8px; background: var(--primary-dark); }
  .ab-products-grid { gap: 24px; }
  .ab-facility-content { background: var(--surface); }
  .ab-cta { background: linear-gradient(135deg, var(--accent), var(--secondary)); }
  .ab-cta-inner { background: rgba(255,255,255,0.1); border: 1px solid rgba(255,255,255,0.22); border-radius: 8px; padding: 42px; backdrop-filter: blur(8px); }

  /* Responsive */
  @media (max-width: 1024px) {
    .ab-empathy-inner { flex-direction: column; gap: 0; }
    .ab-empathy-accent { width: 100%; height: 4px; }
    .ab-identity { grid-template-columns: 1fr; }
    .ab-identity-image { height: 400px; }
    .ab-identity-content { padding: 60px 24px; }
    .ab-delivery-grid { grid-template-columns: 1fr; gap: 50px; }
    .ab-quality-steps { grid-template-columns: 1fr; }
    .ab-quality-step { border-left: none; border-bottom: 1px solid var(--border); }
    .ab-quality-step:last-child { border-right: none; }
    .ab-customer { grid-template-columns: 1fr; }
    .ab-customer-image { height: 400px; }
    .ab-customer-content { padding: 60px 24px; }
    .ab-testimonials-grid { grid-template-columns: 1fr; }
    .ab-transparency-inner { grid-template-columns: 1fr; }
    .ab-products-grid { grid-template-columns: 1fr; }
    .ab-prod-card { border-left: 1px solid var(--border); border-right: 1px solid var(--border); border-bottom: 1px solid var(--border); }
    .ab-prod-card:first-child { border-top: 1px solid var(--border); }
    .ab-section-header { flex-direction: column; align-items: flex-start; gap: 20px; }
    .ab-facility { grid-template-columns: 1fr; }
    .ab-facility-image { height: 400px; order: -1; }
    .ab-facility-content { padding: 60px 24px; }
    .ab-team-inner { grid-template-columns: 1fr; }
    .ab-cta-inner { flex-direction: column; text-align: center; }
    .ab-cta-actions { justify-content: center; }
  }
  @media (max-width: 768px) {
    .ab-hero { min-height: 75vh; }
    .ab-hero h1 { font-size: clamp(26px, 7vw, 42px); }
    .ab-hero-stats { flex-direction: column; gap: 20px; align-items: flex-start; }
    .ab-hero-stat-divider { width: 100%; height: 1px; }
    .ab-quality-coa { flex-direction: column; text-align: center; padding: 24px; }
    .ab-products, .ab-delivery, .ab-quality, .ab-testimonials { padding: 80px 0; }
    .ab-cta { padding: 60px 0; }
    .ab-team-card { flex-direction: column; text-align: center; }
    .ab-empathy { padding: 60px 0; }
  }
</style>
